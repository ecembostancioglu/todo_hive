class Todo{
  final String id;
  final String name;
  final DateTime createdAt;
  final bool isCompleted;

  Todo({
    required this.id,
    required this.name,
    required this.createdAt,
    required this.isCompleted});
}